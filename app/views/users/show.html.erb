<%= render 'books/list', user: @user %>

<h1>Books</h1>

<table>
  <tr>
   <th>本のタイトル</th>
   <th>感想</th>
   <th></th>
  </tr>

  <% @books.each do |book| %>
  <tr>
    <td> <%=book.title %> </td>
    <td> <%=book.body %> </td>
  </tr>
  <% end %>
</table>